import type { MarketAnalysis, MatchData, CalibrationStatus } from '@/types/match';
import { decideNexus, marketsToNexusEvidence, type NexusDecisionOutput, type NexusEvidence, type NexusMode } from '@/lib/nexusDecisionCore';
import { assessDataQuality } from '@/lib/dataQualityGate';
import type { HybridSignal } from '@/lib/hybridEngine';
const HYBRID_CONFIDENCE: Record<HybridSignal['confidence'], number | null> = { alta:90, média:75, padrão:null };
function hybridEvidence(signal:HybridSignal):NexusEvidence[]{const p=signal.daEstimated?0.75:1;return [{source:'engine',name:'hybrid_pressure',value:signal.pressure},{source:'live',name:'shots_on_goal',value:signal.shotsOnGoal*10},{source:'live',name:'corners',value:signal.corners*10},{source:'live',name:'possession',value:signal.possession},{source:'live',name:'dangerous_attacks',value:Math.min(100,signal.dangerousAttacks*5),weight:p}];}
export function adaptHybridSignal(signal:HybridSignal,market?:MarketAnalysis):NexusDecisionOutput{const match:Pick<MatchData,'id'|'homeTeam'|'awayTeam'|'league'|'isLive'|'status'|'minute'>={id:signal.matchId,homeTeam:signal.match.split(' vs ')[0]||signal.match,awayTeam:signal.match.split(' vs ')[1]||'',league:signal.league,isLive:true,status:'LIVE',minute:signal.minute};const markets=market?[{...market,probabilitySource:market.probabilitySource??'HEURISTIC' as const}]:[];const dataQuality=assessDataQuality({live:true,observedAt:signal.observedAt??null,estimatedData:signal.daEstimated});return decideNexus({match,mode:'LIVE' satisfies NexusMode,confidence:HYBRID_CONFIDENCE[signal.confidence],markets,evidence:[...hybridEvidence(signal),...marketsToNexusEvidence(markets)],analysisBlocked:!signal.signalEligible,engineConflict:signal.signalEligible===false&&signal.tier!=='NORMAL',dataQuality});}
export function adaptPreMatch(match:MatchData,markets:MarketAnalysis[],confidence:number|null|undefined,calibrationStatus:CalibrationStatus='UNCALIBRATED'):NexusDecisionOutput{const normalizedMarkets=markets.map(m=>({...m,probabilitySource:m.probabilitySource??'MODEL_ESTIMATE' as const,calibrationStatus:m.calibrationStatus??calibrationStatus}));const sampleSize=match.sampleSize?Math.min(match.sampleSize.homeGames,match.sampleSize.awayGames):null;const dataQuality=assessDataQuality({live:false,sampleSize,requiredSampleSize:3,sourceCompleteness:match.modelData&&match.sampleSize?100:75,requiredFeaturesPresent:normalizedMarkets.length>0});return decideNexus({match,mode:'PRE_MATCH',confidence,markets:normalizedMarkets,evidence:marketsToNexusEvidence(normalizedMarkets),dataQuality,calibrationStatus,probabilitySource:'MODEL_ESTIMATE'});}

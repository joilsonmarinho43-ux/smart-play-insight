import Index from './Index';

/**
 * World Cup-specific presentation is intentionally delegated to the canonical
 * analyst pipeline until a competition-specific Core model exists.
 */
export default function WorldCup() {
  return <Index />;
}
